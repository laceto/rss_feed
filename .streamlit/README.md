# Streamlit Cloud Secrets

Configure the following secret in the Streamlit Cloud dashboard under App Settings > Secrets:

```toml
OPENAI_API_KEY = "sk-..."
```

Never commit `secrets.toml` to this repository.
